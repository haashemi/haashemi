import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Project Details | Ali Hashemi",
  robots: { index: false },
};

const Project = () => <div className="flex h-[70vh] items-center justify-center">Not implemented, yet.</div>;

export default Project;
