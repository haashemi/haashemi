import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog Post | Ali Hashemi",
  robots: { index: false },
};

const BlogPost = () => <div className="flex h-[70vh] items-center justify-center">Not implemented, yet.</div>;

export default BlogPost;
