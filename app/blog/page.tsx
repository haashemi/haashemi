import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog Posts | Ali Hashemi",
  robots: { index: false },
};

const BlogPosts = () => <div className="flex h-[70vh] items-center justify-center">Not implemented, yet.</div>;

export default BlogPosts;
